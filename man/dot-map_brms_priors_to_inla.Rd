% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-data-model.R
\name{.map_brms_priors_to_inla}
\alias{.map_brms_priors_to_inla}
\title{Map brms Priors to INLA Priors}
\usage{
.map_brms_priors_to_inla(priors, approx_t_as_normal = TRUE)
}
\arguments{
\item{priors}{A \pkg{brms} \code{prior} object or data.frame.}

\item{approx_t_as_normal}{Logical; if TRUE, Student-t priors with df > 2 are approximated by a Normal.}
}
\value{
A list with:
\describe{
\item{control_fixed}{Named list for \code{control.fixed} in INLA.}
\item{hyper_by_re}{Reserved; list for hyperparameters by RE (empty by default).}
}
}
\description{
Parses a \pkg{brms} prior specification and maps fixed-effect priors to INLA's
\code{control.fixed} list elements.
}
\details{
Supports \code{normal()} and \code{student_t()} (optionally variance-matched to Normal if df > 2).
}
\keyword{internal}
