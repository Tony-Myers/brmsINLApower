% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-generator.R
\name{.auto_data_generator}
\alias{.auto_data_generator}
\title{Automatic Data Generator for brms + INLA Simulation}
\usage{
.auto_data_generator(
  formula,
  effect_name,
  family = gaussian(),
  family_args = list(),
  error_sd = 1,
  group_sd = 0.5,
  obs_per_group = 10,
  predictor_means = NULL,
  predictor_sds = NULL
)
}
\arguments{
\item{formula}{Model formula (may include brms-style random effects).}

\item{effect_name}{Name of the fixed effect to manipulate.}

\item{family}{brms family object, e.g., \code{gaussian()}, \code{binomial()}.}

\item{family_args}{Named list of family-specific arguments, e.g.
\code{theta} for negbinomial, \code{phi} for beta, \code{size} for binomial,
\code{E} for poisson exposure, \code{df} for Student-t, \code{shape} for Weibull.}

\item{error_sd}{Residual SD for Gaussian-like families.}

\item{group_sd}{SD of random effects.}

\item{obs_per_group}{Number of observations per grouping level.}

\item{predictor_means}{Optional named list of means for continuous predictors.}

\item{predictor_sds}{Optional named list of SDs for continuous predictors.}
}
\value{
A function \code{function(n, effect)} returning a data.frame of simulated data.
}
\description{
Creates a function that generates synthetic data for each simulation replicate
based on a \pkg{brms}-style model formula, an effect name, and a GLM family,
producing columns and structures compatible with \pkg{INLA}.
}
\details{
The returned generator function has signature \code{function(n, effect)}, which
produces a dataset of size \code{n} with the target effect set to \code{effect}.
It automatically adds grouping factors, predictor variables, random effects,
and family-specific response generation (including .Ntrials, .E, .scale columns
where required by INLA).
}
\keyword{internal}
