% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-helpers.R, R/utils-helpers.R
\name{.compute_assurance}
\alias{.compute_assurance}
\title{Compute Mean Assurance for a Given Metric}
\usage{
.compute_assurance(
  df,
  metric,
  prob_threshold,
  rope_rule = c("≥"),
  direction_p = 0.5
)

.compute_assurance(
  df,
  metric,
  prob_threshold,
  rope_rule = c("≥"),
  direction_p = 0.5
)
}
\arguments{
\item{df}{Data frame containing simulation results with columns \code{n},
\code{true_effect}, \code{ok}, and a metric column.}

\item{metric}{Character; one of "direction", "threshold", "rope".}

\item{prob_threshold}{Numeric; probability threshold for \code{"threshold"} and \code{"rope"} metrics.}

\item{rope_rule}{Reserved for future use (e.g. >= vs <= inside-ROPE rules).}

\item{direction_p}{Probability cutoff for "direction" metric (default 0.5).}
}
\value{
A tibble grouped by \code{n} and \code{true_effect} with column \code{assurance}, the mean of passing runs.

A tibble grouped by \code{n} and \code{true_effect} with column \code{assurance}.
}
\description{
Given simulation results, compute the mean assurance for a specified decision rule metric.

Internal function to summarise simulation results and compute the
proportion of passes for a given decision rule metric.
}
\keyword{internal}
